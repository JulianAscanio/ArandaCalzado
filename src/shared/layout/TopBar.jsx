export default function Topbar({ title }) {
    return (
        <header className="topbar">
            <div>
                <h1>{title}</h1>
                <p>Aranda Calzado</p>
            </div>
        </header>
    )
}